# Discord

The bot uses modern Discord application commands and Components V2.

Only two commands exist:

    /config
    /status

/config uses:

- LayoutView
- Channel Select
- Role Select
- ephemeral responses
- Make Channel
- Enable/Disable

Message Content intent is not required.
